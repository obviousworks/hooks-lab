# Your Claude Code hook scripts go here

Empty on purpose. Task A onwards fills it.

The scripts live here, the wiring lives in `.claude/settings.json` one level up.
Run `/hooks` inside a session to see what Claude Code actually loaded, and from
which file.

Verify what you build with:

```bash
scripts/verify-hooks.sh claude
```
